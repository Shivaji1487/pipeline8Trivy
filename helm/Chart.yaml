apiVersion: v2
name: pipeline10java
description: A Helm chart for the Pipeline8 with trivy DevOps project
type: application

# This is the chart version (internal to Helm)
version: 0.1.0

# This is the version of the application being deployed (your app version)
appVersion: "1.0.0"