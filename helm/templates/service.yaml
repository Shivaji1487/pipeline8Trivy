apiVersion: v1
kind: Service
metadata:
  name: pipeline8-trivy-service # All lowercase
  namespace: bank-production-v8
spec:
  type: ClusterIP
  selector:
    app: pipeline8-app # Must match Deployment metadata.labels.app
  ports:
    - port: 80
      targetPort: 80